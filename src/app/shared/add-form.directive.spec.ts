import { AddFormDirective } from './add-form.directive';

describe('AddFormDirective', () => {
  it('should create an instance', () => {
    const directive = new AddFormDirective();
    expect(directive).toBeTruthy();
  });
});
